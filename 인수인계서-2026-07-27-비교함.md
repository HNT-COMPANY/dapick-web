# 다픽(Dapick) 인수인계서 — 2026-07-27 비교함 라운드

작성 2026-07-27 · 다음 채팅창은 **이 문서 하나만 읽으면** 이어서 작업 가능하게 썼다.
같이 낸 것: `KPI-2026-07-27-비교함.csv`(시트 붙여넣기용, 54건), `KPI-2026-07-27-비교함.md`, `.agent/memory.md` CURATE 추가분(이미 반영됨).

---

## 0. 한 줄 요약

비교함(상품 비교) 기능 A·B·C·D 네 덩어리 전부 구현 완료. 코드는 기기에 전달·md5 검증 끝. **커밋은 안 했다**(규칙: 커밋·푸시는 사용자 본인). 회귀 검사 `test-compare.js` 164/164 통과, 다른 스위트 합계 약 896 pass / 1 fail(비교함과 무관한 기존 실패 1건).

---

## 1. 세션 시작 시 읽을 순서 (닫힌 학습 루프 — READ)

에이전트 브레인은 **`dapick-network/.agent/`** 에 중앙집중돼 있다. 다음 세션은 이 순서로 읽는다.

1. 해당 레포 `CLAUDE.md` — 협업 규칙·금지사항·네이밍
2. `dapick-network/.agent/profile.md` — 사용자 작업 스타일
3. `dapick-network/.agent/memory.md` — 결정 로그·교훈 로그·열린 질문 (이번 세션분 append 완료)
4. `dapick-network/.agent/skills/` — 플레이북 (`README.md`, `카드-필드-추가.md`)
5. 해당 레포 `구조지도_backend.md` / `구조지도_admin.md` / `구조지도_web.md`
6. `협업규칙-KPI.md` (network·admin 두 곳에 동일본) — KPI 작성 양식

작업이 끝나면 **CURATE**: 결정 → memory.md 결정 로그, 함정 → 교훈 로그, 남은 일 → 열린 질문. `.agent/` 파일도 사용자가 직접 커밋.

⚠️ `dapick-web` 과 `dapick-admin` 에는 `.agent/` 폴더가 **없다**. 형제 폴더인 `dapick-network/.agent/` 를 읽으라는 뜻이다.

---

## 2. 프로젝트 좌표

| 저장소 | 브랜치 | HEAD | 상태 | 성격 |
|---|---|---|---|---|
| `dapick-web` | `develop` | `3c94b2f` | **미커밋 10 M + 2 ??** | 정적 vanilla JS, 빌드 없음. Cloudflare Pages 고급 모드(단일 `_worker.js` + HTMLRewriter) |
| `dapick-admin` | `main` | `ece5781` | clean | 정적 vanilla JS |
| `dapick-network` | `develop` | `ee42fd2` | clean | Spring Boot 3.2.4 / Java 17 / JPA 6.4.4 / Flyway 9.22.3 / PG16, 포트 8081 |

기기 경로: `C:\Users\user\OneDrive\Desktop\dapick-{web,admin,network}` (컨테이너에서는 `/sessions/rcw-.../mnt/<폴더>`).

- 다픽은 파이브엠·다페이와 **완전 별개**. 코드·자산·컨벤션 섞지 않는다. (memory.md 는 '다정산'을 별개 신규 프로젝트로 적어두고 있다 — 다픽 자산 직접 사용 금지, 패턴만 참고.)
- Java 패키지 루트 `com.dapick.api` (`com.dapick.network` 아님).
- `SecurityConfig.java` 는 `com/dapick/api/config/` (`global/config` 아님). 78행 `.requestMatchers("/oauth2/**").permitAll()`, 69행 `POST /api/auth/**` permitAll.
- `ddl-auto: validate` — 엔티티 컬럼 추가 시 **Flyway 마이그레이션 동시 작성 필수**. 네이밍 `V{YYYYMMDD}{NNN}__{snake_case}.sql`, 위치 `src/main/resources/db/migration/`. `outOfOrder` 켜져 있음.
- 로컬: PostgreSQL **:5433**, 백엔드 **:8081**, 정적 프론트 Live Server **:5500**. IntelliJ(백엔드) + VS Code(프론트), Windows.
- `config.js` hostname → API base: localhost / 127.0.0.1 / 192.168. / 10. / .local → `http://localhost:8081`, 그 외 → `https://api.dapick.co.kr`.
- 운영: EC2 systemd 서비스 `dapick`, `ubuntu@3.34.73.240` / `api.dapick.co.kr`, 배포 `deploy.ps1`(gradlew clean build → scp jar → systemctl restart). env 는 유닛 인라인 `Environment=` 방식이라 수정 후 `daemon-reload` 필수.
- 배포 캐시: `_headers` 없음, `_worker.js:483` 이 `cache-control: public, max-age=600`. `<script src>` 에 버전 쿼리가 없어 **배포 후 약 10분간 Ctrl+F5 필요**.
- CORS 는 `CorsConfig.java` 와 `SecurityConfig.corsConfigurationSource()` 두 곳 — 둘 다 `setAllowedOriginPatterns` 로 로컬 포트 와일드카드 허용으로 통일돼 있다. 한쪽만 고치면 안 된다.

### 제품 컨셉 (유지)
> 관리자 페이지에서 시작해서 운영자 페이지에 도달한다 → 그걸 수익화로 돌리기 위한 목적성을 가진다.
> 레이아웃은 사용자가 정하고 코드를 짜고, CSS 디자인·디테일은 시니어(어시스턴트)가 만든다.

### 커밋 컨벤션
`feat(scope): 한국어 설명` / `fix(...)` / `refactor(...)` / `chore(...)`. co-author 트레일러 없음. 흐름은 **백엔드 → 어드민 → 웹**.

---

## 3. 비교함 — 전체 그림

카테고리 3개가 각자 독립 서랍: `INTERNET_TV`, `WATER`, `RENTAL`. 서로 안 섞인다(사용자 요구: "인터넷 tv는 인터넷 tv만"). 핵심 가치는 **월 요금 비교**.

저장: `localStorage['dapick_compare']` = `{ WATER: [...], RENTAL: [...], INTERNET_TV: [...] }`. 카테고리당 최대 3개.

항목 모양:
```js
{ category, id, key, name, model, image, label, monthlyFee, options }
```
`key = keyOf(id, options)` — **"상품 + 조합"이 하나의 항목**. 같은 정수기라도 3년/6년이면 다른 줄.

### 덩어리별
- **A. 찜 조합 복원** — 완료. `mpRecentUrl()` 이 `options` 를 쿼리스트링으로 실어 보내고 상세가 `WD_WANT`/`RD_WANT` 로 복원.
- **B. 카테고리별 비교함 + 마이페이지 비교함 화면** — 완료. 표 첫 행 월 요금 고정, 최저가 강조, 차액 표시.
- **C. 하단 3칸 트레이** — 완료.
- **D. 트레이 '+' 카드에서 그 자리로 고르기** — **이번 라운드. 전달·검증 완료.**

---

## 4. 이번 라운드(D) 상세

사용자 요구 원문:
> "비교하기 버튼 누르니까 http://localhost:5500/internet.html 이동하는데요 이러면 비교함에 상품을 담을 수 없습니다. 비교할동안에는 +버튼을 누르면 그 자리에서 상품을 선택하도록 만들어야 합니다"

**원인.** C에서 `slotEmpty()` 가 `dpListUrl(cat)` 로 `<a href="internet.html">` 을 그렸다. '+' 를 누르면 목록으로 나가버려 담아둔 걸 두고 화면을 떠난다 → 비교 흐름이 끊긴다.

**해결.** '+' 카드를 **그 자리에서 열리는 상품 고르기 시트**로 교체.

### 4-1. 구조 결정 — provider 등록 방식 (선택 A)

`compare-view.js` 가 `registerPicker(cat, fn)` 을 열고, **각 상세 페이지가 자기 카테고리 목록 공급 함수를 등록**한다. 공급 함수는 `Promise<Array<row>>` 반환.

버린 대안: `compare-view.js` 안에 카테고리 분기. 그러면 공용 파일 하나가 세 가지 가격 구조(정수기·렌탈 = `pricing[약정][주기][조건]` 3단 맵, 인터넷 = `internetOptions` 배열 + `InternetCalc` 계산기)를 전부 알아야 하고, 카테고리가 늘 때마다 공용 파일을 고치는 구조가 된다. 사용자가 이미 지적한 덫이다 — "가맹점이 10개 100개가 될 때도 계속 사이트맵 줄을 고칠 건가요?"

### 4-2. 질문 없이 내린 판단 두 개 — **가부 확인 필요**

**(가) 담는 조합 = "지금 화면과 같은 조합", 없으면 그 상품의 가장 싼 조합.**
근거: 핵심이 월 요금 비교인데 내 화면이 3년·자가관리인데 상대를 6년·방문관리로 담으면 숫자가 나란히 놓여도 사과 vs 오렌지다. 대신 **고른 조합을 목록 줄에 그대로 적어** 감추지 않는다. 색상도 같은 규칙(그 상품에 그 색이 있으면 따라가고, 없으면 그 상품의 첫 색).

**(나) 인터넷·TV 목록 알갱이 = 통신사 × 인터넷 옵션 하나(TV·공유기 없는 인터넷 단독가).**
근거: TV·공유기 조합까지 넣으면 목록이 수백 줄. 여기서 기본 요금제를 고르고 세부는 '자세히 보기'로 상세에서 다듬는다.

### 4-3. 전달 완료 파일 (5개, md5 기기 확인됨)

| 파일 | 줄 수 | md5 | 개행 |
|---|---|---|---|
| `js/common/compare-button.js` | 266 | `234403772d08c1870b2682a2859f1e08` | LF |
| `js/common/compare-view.js` | 637 | `b713339a38e0bad5500d6e5dd4f364d8` | LF |
| `js/water/water-detail.js` | 1014 | `5a0940f65967c7eddfef86b9d90d868f` | **CRLF 전체** |
| `js/rental/rental-detail.js` | 769 | `b11d1c1aae188e5c0dab371aaf0435d4` | **CRLF 전체** |
| `js/internet/internet-detail.js` | 827 | `d1451399b0a4884571250e6033d3d8d9` | LF |

**`compare-button.js`** — `addItem(x)` 신설 + 공개(`dpCompareStore.add`). 상세 버튼의 `toggle()` 도 여기를 지난다. **담는 길을 하나로 만든 이유**: 3개 제한과 중복 검사가 두 벌이 되면 반드시 어긋난다. 반환 `{ ok, reason }`, reason 은 `'bad' | 'dup' | 'full'`. `has()` 도 공개.
⚠️ `notify()` 를 `write()` 안에 넣지 말 것 — `read()` 의 옛 형식 이관 경로를 타고 재귀한다.

**`compare-view.js`** — 신규 '상품 고르기 시트' 섹션. `_pickers` 맵, `registerPicker`/`openPicker`/`renderPicker`/`loadPicker`/`closePicker`. `slotEmpty()` 은 `_pickers[cat]` 이 있으면 `<button data-pick="1">`, 없으면 **예전 `dpListUrl` 링크로 폴백**(아직 안 붙인 페이지 보호). CSS 는 `injectStyles()` 안, `.dp-pick-ov` **z-index 9600**.
`loadPicker()` 가 공급 결과를 `r.category === cat` 으로 걸러낸다 — 공급 함수가 버그로 딴 카테고리를 줘도 서랍이 안 섞이는 구조적 방어.

**`water-detail.js`** — `wdComboFee` / `wdPickCombo`(정확 일치 → 없으면 fee>0 최소) / `wdPickRow` / `wdRegisterPicker`. `GET /api/water-products` 전체. 기준 조합은 `#wdContract`/`#wdCycle`/`#wdType`/`WD_COLOR`.

**`rental-detail.js`** — `rdPickCombo`/`rdPickRow`/`rdRegisterPicker`. `GET /api/rental-products?categoryId=` — **같은 렌탈 카테고리 안에서만**(안마의자는 안마의자끼리). 등록 호출은 `mountCompare()` 두 군데 모두(가격 없는 분기 ~174행, 정상 분기 ~203행). 가격 정보 없으면 `monthlyFee: 0` → '상담 시 안내'.

**`internet-detail.js`** — `logoOf(key)`(로고 절대경로화; 마이페이지가 루트 기준으로 같은 자산을 쓴다) + `registerPicker()`. `GET /api/internet-tv-products` → 통신사 × `InternetCalc.extractOptions(p).internets` 한 줄씩, 요금은 `InternetCalc.calculate({internet, toggles:{}, meta}).finalPrice`. `CARRIER_MAP` 에 없는 통신사는 로고·표시명이 없어 제외. `initCompare()` 의 로고 처리도 `logoOf` 로 합침.

### 4-4. 시트 UX
불러오는 중 안내 / 실패 시 '다시 시도' / 이름·조합·모델명 검색(다시 그려도 포커스·커서 유지) / 이미 담긴 줄은 `담김` 비활성 / 3개 찼는데 담으면 "비교는 3개까지입니다" 한 줄 안내 / 3칸 차면 자동 닫힘 / 배경 클릭·Esc 닫기.

### 4-5. z-index 지도
```
9998  .idlg-overlay      인터넷 옵션 다이얼로그
9600  .dp-pick-ov        상품 고르기 시트   ← 이번 추가
9500  .dp-sheet-ov       비교 시트
1200  .dp-tray           하단 트레이
1000  water.css 필터 서랍 / 999 오버레이 / 900 .wf-toggle
 500  common.css .modal-overlay
```

---

## 5. 검증 결과

`/root/verify/` (컨테이너, jsdom).

| 스위트 | 결과 |
|---|---|
| `test-compare.js` | **164 / 0** (picker 섹션 신규 포함) |
| `test-fav-restore.js` | 32 / 0 |
| `test-fav-internet.js` | 88 / 0 |
| `test-fav-water.js` | 45 / 0 |
| `test-fav-join.js` | 14 / 0 |
| `test-fav-thumb.js` | 24 / 0 |
| `test-sitemap.js` | 25 / 0 |
| `test-store.js` | 233 / 0 |
| `test-store-detail.js` | 151 / 0 |
| `test-worker-parity.js` | 95 / 0 |
| `test-mobile.js` | 25 / **1 fail** |

**남은 실패 1건**: `test-mobile.js:132` "detailUrl 없으면 준비중 안내". `openStoreModal('dapon-mugeo')` → `goStoreDetail()` 이 '오픈 예정' alert 를 안 띄운다. 비교함과 무관한 기존 실패이고, 매장 상세를 정적 파일이 아니라 데이터로 생성하도록 바꾼 `6bb1810` 이후 테스트가 낡았을 가능성이 크다. **진짜 회귀인지 낡은 테스트인지 아직 결론 안 냈다.**

이번에 고친 테스트 2건(코드 버그 아님, 테스트가 틀렸던 것):
1. `okFetch()` 가 모든 fetch 에 목록을 돌려줘 `loadDetail('/api/water-products/p-1')` 이 '빈 응답'으로 넘어지며 화면을 갈아엎었다 → 목록 URL 에만 응답하도록 좁힘.
2. 첫 줄(지금 보고 있는 상품)은 지금 조합으로 이미 담겨 있으니 `담김` 이 맞고, 색도 지금 화면(화이트)을 따라가는 게 맞다 → 기대값을 코드 동작에 맞춰 정정.

---

## 6. 사용자가 지금 할 일

1. **로컬 확인** — Live Server 로 `water-detail.html` / `rental-detail.html` / `internet-detail.html` → 비교하기 → 하단 트레이 '+' → 시트에서 담기. **Ctrl+F5 한 번**.
2. **커밋** (자동 실행 금지, 본인이):
   ```
   feat(compare): 트레이 + 카드에서 그 자리로 상품 고르기
   ```
   미커밋 변경에는 A·B·C 분도 함께 있다: `mypage.js`, `mypage.html`, `internet-detail.html`, `css/internet/internet-detail.css`, `water-detail.html`, `rental-detail.html`, 신규 `js/common/compare-view.js`·`js/common/product-url.js`. 한 커밋으로 묶을지 쪼갤지는 판단.
   ※ `dapick-network/.agent/memory.md` 도 이번에 CURATE 추가분이 들어갔다 — 따로 `chore(agent): 세션 로그 갱신` 등으로 커밋.
3. **4-2의 판단 두 개** 가부.
4. KPI 는 같이 낸 CSV 를 daily 시트에 붙여 쓰면 된다(54건, 40건 기준 충족).

---

## 7. 결정 대기 중

1. **옛 찜 데이터 `option_key = ''`.** 정수기·렌탈 기존 찜 행은 `option_key` 가 빈 문자열인데 조합 단위 찜은 실제 키가 들어간다 → 옛 찜이 "찜 안 됨"으로 읽힌다. **코드로 못 고치는 데이터 문제.** (가) 그냥 둔다(사용자가 다시 찜) (나) 옛 행 삭제 마이그레이션. 답 못 받음.
2. **비교함이 로컬 전용.** 기기·브라우저 바꾸면 사라진다. 서버 API 로 옮길 자리는 `compare-button.js` 의 `read()`/`write()` 한 쌍뿐.
3. **`CARRIER_MAP` 5중 복제.** dapick-web 5개 파일에 같은 통신사 표(`internet-detail.js` ~19행에 자기 복사본: SKT/KT/LG U+/LG HelloVision/SK broadband/KT Skylife 의 `{name,color,logo}`). 한 파일로 뽑을지 미결.
4. **UI 개편 범위 미정** — 렌탈 select→버튼박스 통일 / 패널 재배치 / 장식용 이모지(📅🔧🏷️🎨) 제거.
5. **렌탈 카테고리 개편** — 사용자 수정 스펙 대기(보류).

---

## 8. 다음 라운드 후보 (제안 우선순위)

1. `test-mobile.js` 실패 1건 진단 — 진짜 회귀인지 낡은 테스트인지.
2. 목록 카드 옛 하트 정리 — `js/water/water.js:381` `.water-card-heart` 의 `quickFav()` 제거 또는 새 찜 버튼으로 전환.
3. `water.js` 의 `[모델명 디버그]` `console.log` 제거.
4. 지역 변수 `favorites` → `dialogPrefs` 개명(전역 찜과 이름 충돌로 읽기 나쁨).
5. 배포 캐시 개선 — `_headers` 추가 또는 `<script src>` 버전 쿼리. 지금은 배포마다 Ctrl+F5 안내가 필요하다.
6. 네이버 간편 로그인 (11절).
7. `#9 PENDING_PROFILE` 약관 재노출 — `daconnect33@gmail.com` 계정이 막혀 있음.
8. `#13` 나머지 카테고리 페이지 확장 / `#18` 장식용 이모지 제거.
9. `resumePendingKakaoConsult()` 가 `js/water/water.js:1036` 에서만 호출됨 — 렌탈·인터넷은 로그인 후 상담을 이어받지 못한다.
10. **보안 백로그**: `application.yml` 에 `jwt.secret` / `kakao.rest-api-key` 가 평문 커밋돼 있다(memory.md 기록). env 분리 권장. ※ 사용자는 "시크릿키는 재발급 안 받는다"고 명시했으므로 분리 방식만 제안할 것.

**조건부/보류**
- `legacy-static/` 삭제는 매장 7개 지점 등록 후.
- 매장 7개 등록은 사용자가 직접. `/api/stores` 아직 비어 있음.
- 배포 확인: `/store-byeongyeong` 이 아직 옛 상세를 준다는 보고 → Pages 빌드가 `6bb1810` 이후로 돌았는지, `dapick.co.kr/sitemap.xml` 확인.

**백엔드/어드민 백로그**
렌탈 `attributes` jsonb + Flyway + DTO + 관리자 UI / `ProductAdminResponse` DTO 확장 / `AdminNotifyPhone.active`·`sortOrder` `@Builder.Default` / `deploy.ps1` 라벨 / Quill 글꼴 저장소 + px 폰트 크기 / 안 쓰는 `StoreResponse.detailUrl`·`stores.detail_url` 제거 / `StoreResponse` 에 `updatedAt` 추가.

**사용자가 직접 지워야 하는 것** (도구가 파일 삭제 불가): `dapick-admin/_to_delete/`, `dapick-network/_to_delete/`, `dapick-web/_to_delete/`.

---

## 9. 함정 모음

### 도구·환경
- **기기 경로가 도구마다 다르다.** `device_bash` = `/sessions/rcw-.../mnt/<폴더>`, `device_stage_files`/`device_commit_files` = 윈도우 절대경로 `C:\Users\user\OneDrive\Desktop\dapick-{web,admin,network}`.
- **파일 전달 절차**: `SendUserFile`(→ `file_uuid`) → `device_commit_files`(윈도우 절대경로, `force: true`) → **반드시 `device_bash` 로 md5 재확인**. 예전에 커밋이 조용히 실패("bridge 미연결")했는데 완료라고 보고했다가 "계속 똑같이 메인으로 오는데요?" 를 들었다. **md5 확인 없이 완료 보고 금지.**
- `device_bash` 는 **파일 삭제 불가**(`rm` → Operation not permitted). `mv` 로 `_to_delete/` 에 옮기고 알린다.
- `device_bash` 로 **git 쓰기 금지**(index.lock). `--no-optional-locks` 읽기 명령만 예외.
- `Read` 도구는 기기 경로를 못 연다 — `device_bash cat` 또는 스테이징 먼저.
- `device_stage_files` 는 **이미 스테이징된 경로를 덮어쓰지 않는다** — 먼저 `rm -f`.
- `Edit` 도구는 **이번 세션에서 `Read` 도구로 읽은 파일만** 고칠 수 있다. Bash 의 `sed`/`grep` 로 본 건 카운트 안 된다. 셸로 in-place 수정(`sed -i`, `cat >`)했으면 `Edit` 전에 다시 `Read`.
- 큰 편집은 python `.replace(..., 1)` + `assert count==1` 형태로 — 브리지가 끊겨도 부분적용이 안 남는다(memory.md 07-20 기록).
- **질문 카드(AskUserQuestion)로 막지 말 것.** 예전에 이걸로 "30분 넘게 답이 없네요 무슨 말이에요" 를 들었다. 물어볼 게 있으면 산문으로 묻고 일단 진행.

### 코드·테스트
- **개행이 파일마다 다르다.** `water-detail.js`(1014/1014), `rental-detail.js`(769/769)는 **전부 CRLF**. `compare-button.js`·`compare-view.js`·`internet-detail.js`·`product-url.js`·`mypage.js` 는 LF. 정규화하면 통짜 diff 가 된다.
- **jsdom**: `window.eval()` 마다 최상위 `let`/`const`/`function` 스코프가 따로 논다. 스크립트를 **하나로 이어붙여 한 번에** eval 하고, 끝에 `window.__set`/`window.__get` 을 direct eval 로 노출해야 내부 변수를 만질 수 있다. 예외: `product-url.js` 처럼 `window.*` 에 명시적으로 대입하는 IIFE 는 따로 eval 해도 된다.
- jsdom 은 `/root/verify/` 에서만 resolve 된다.
- `node --check` 만으로는 부족(빈 파일도 통과) — 행동 테스트와 짝지을 것.
- 검사용 가짜 `fetch` 는 **URL 로 분기**시킬 것. 전부 같은 응답을 주면 상세 로더가 넘어지며 화면을 갈아엎어 그 뒤 검증이 통째로 헛돈다(이번에 실제로 당함).
- Playwright 미리보기는 파일명이 `water-preview.html` 이어야 한다 — `simple-apply.js` 가 `location.pathname` 에 rental/water/internet 이 없으면 조기 return.
- `pkill -f "http.server 8899" || true` 를 다른 명령과 이어 쓰면 exit 144 로 뒤따르는 `nohup` 이 죽는다. `nohup python3 -m http.server 8899 &` 는 단독 줄로.
- 한 경로의 e2e 통과로 다른 경로가 된다고 결론짓지 말 것(로그인 복귀 버그에서 그렇게 틀렸다).

### 백엔드
- `favorites` 테이블에 create-table 마이그레이션이 **없다**. 옛 unique 제약 제거는 `pg_constraint`(contype='u')와 `pg_index`(indisunique AND NOT indisprimary)를 **둘 다** `DO $ ... $` 안에서 훑어야 한다. 파일 `V20260727001__add_favorite_options.sql`, `option_key NOT NULL DEFAULT ''`.
- jsonb 패턴: `@JdbcTypeCode(SqlTypes.JSON)` + `@Column(columnDefinition="jsonb")`. 타입 있는 POJO 값 객체는 **`@Setter` 필수**.
- `DapickException`(`com.dapick.api.global.exception`)은 **abstract 아니다**.
- 엔티티 빌더/update 시그니처에 새 파라미터는 **맨 끝에 append**, 저장 후 인자 개수 일치 검증(memory.md 카드 교훈).
- `NotificationType` 은 `KakaoAlimtalkSender` 가 default 없는 exhaustive switch → enum 값 추가 시 **case 추가 필수**.
- `api.js`: `api.get` 은 `ApiResponse.data` 를 언랩해 객체를 그대로 반환.

### 도메인 모양 차이
- 정수기·렌탈 = '상품 하나 = 화면 하나' (`water-detail.html?id=`, `rental-detail.html?id=`).
- 인터넷·TV = 통신사 하나가 Product 하나, 조합은 URL 파라미터 (`internet-detail.html?carrier=`).
- 이 차이 때문에 공용 파일에 가격 로직을 넣으면 안 된다(4-1).

---

## 10. 협업 규칙 (반드시 지킬 것)

- **git commit / push 자동 실행 금지.** 커밋·푸시는 사용자 본인. `.agent/` 파일도 마찬가지.
- **운영 서버(EC2) 직접 조작 금지** — 명령만 제시, 실행은 사용자.
- 비밀키·토큰을 코드·로그에 남기지 않는다. (카카오 키 1건은 사용자가 명시적으로 예외 처리, 재발급 안 함 — 그 키에만 해당.)
- 수정 전 **정찰 먼저**, 한 파일씩, **무엇을 왜 바꿨는지 diff 로** 설명.
- 큰 기능은 **방향(A/B) 먼저 묻고** 진행. 단, 범위 되묻기로 시간 끌지 말 것 — 예전에 찜 버튼 위치를 되물었다가 "아니 하 ... 하트를 삭제하고 그 자리에 찜하기 버튼을 만들라구요" 를 들었다.
- 사용자 입력을 `innerHTML` 에 넣을 땐 무조건 `escapeHtml`/`escapeAttr` 경유.
- JS 는 `.js` 파일에만. HTML 에 `<script>` 직접 넣지 말 것.
- **가맹점 수에 비례해 손으로 고치는 구조 금지** — "가맹점이 10개 100개가 될 때도 계속 사이트맵 줄을 고칠 건가요?"
- slug 는 관리자가 직접 입력 — 자동 로마자 변환 다시 꺼내지 말 것.
- 말투: 한국어, 간결, 사족·과한 칭찬 없이, "박다" 계열 동사 지양.

---

## 11. 네이버 간편 로그인 — 준비된 것 전부 (미착수)

**결정 대기 A/B**
1. `KakaoOAuth2Service` 복제 vs `SocialOAuth2Service` 추상 클래스로 추출.
2. `state` 보관 = `HttpSession` vs HttpOnly/SameSite=Lax/5분 쿠키.

**사전 조건**: 네이버 개발자센터 앱 등록(사용자).
**콜백**: `http://localhost:8081/oauth2/callback/naver`, `https://api.dapick.co.kr/oauth2/callback/naver`
**엔드포인트**: `https://nid.naver.com/oauth2.0/authorize`, `https://nid.naver.com/oauth2.0/token`, `https://openapi.naver.com/v1/nid/me`
**새 파일**: `NaverUserInfo.java`, `NaverOAuth2Service.java`, `NaverApiClient.java`
**수정**: `OAuthController.java` + yml 3개

---

## 12. KPI 재료 (수치)

- 이번 라운드 변경 규모: 미커밋 diff 기준 10개 파일 **+656 / -95**, 신규 2개(`compare-view.js` 637줄, `product-url.js` 63줄).
- 비교함 회귀 검사: `test-compare.js` **164 assertion**. 전체 스위트 합계 **약 896 pass / 1 fail**.
- KPI 시트용 항목: 같이 낸 CSV 에 **54건** 정리(양식 5원칙 준수, 최소 40건 충족).
- 성과 지표 후보(현재 계측 미부착): 비교함 담기 수 / 비교 시트 열람 수 / 비교 후 상담신청 전환율 / 카테고리별 담기 분포 / '+' 고르기 시트를 통한 담기 비율(= 이번 라운드가 실제로 흐름을 살렸는지). 계측을 붙인다면 `dpCompareStore.add()` 한 지점과 `openPicker()` 한 지점이면 전부 잡힌다 — 담는 길을 하나로 만들어 둔 덕이다.

---

## 13. 사용자 메시지 원문 보존 (의도 왜곡 방지)

- "우리 컨셉 : 관리자 페이지에서 시작해서 운영자 페이지 도달한다 -> 그걸 수익화로 돌리기 위한 목적성을 가진다. 레이아웃은 내가 정하고 코드를 짜고 그 CSS디자인이나 디테일은 시니어가 만든다. 이 컨셉을 유지하면서 갑니다."
- "비교함은 인터넷 tv는 인터넷 tv만 정수기는 정수기만 렌탈은 렌탈만 / 월 요금 비교가 핵심입니다."
- "비교함 버튼을 누르면 아래에서 3칸이 나오고 그걸 비교할 수 있도록 하는거 가능하죠? +모양 카드 3개 나오고 그 카드 안에서 비교하도록 하는거에요."
- "비교하기 버튼 누르니까 http://localhost:5500/internet.html 이동하는데요 이러면 비교함에 상품을 담을 수 없습니다. 비교할동안에는 +버튼을 누르면 그 자리에서 상품을 선택하도록 만들어야 합니다"
- "아!! 찜하기 비교하기 버튼 이미지 밑에 붙힙시다 이미지 1 여백에 붙힙시다!"
- "가맹점이 10개 100개가 될때도 계속 사이트맵 줄을 고칠건가요?"
